#include<bits/stdc++.h>
using namespace std;
#define int long long

signed main() {
    int n;
    cin >> n;
    vector<int> v(n); // Need not be sorted
    for(int i = 0; i < n; i++)
        cin >> v[i];
}